name: Fine Tune Story Summariser
on:
  push:
    branches:
      - the-one
    paths:
    - beam-llm-hosting-storysummariser/*.py
    - beam-llm-hosting-storysummariser/*.txt
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run Docker-Image
      run: docker build --build-arg BEAM_TOKEN=${{secrets.BEAM_TOKEN}} --name=todeploy beam-llm-hosting-storysummariser/Dockerfile && docker run todeploy
