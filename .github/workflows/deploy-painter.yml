name: Painter Deployment
on:
  workflow_dispatch: {}
  push:
    branches:
      - the-one
    paths:
    - beam-llm-hosting/shared/*.py
    - beam-llm-hosting/painter/*.py
    - beam-llm-hosting/painter/*.txt
    - beam-llm-hosting/painter/Dockerfile
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - name: Run Docker-Image
      run: docker buildx build --build-arg BEAM_TOKEN=${{secrets.BEAM_TOKEN}} --tag=todeploy -f ./beam-llm-hosting/painter/Dockerfile . && docker run todeploy
